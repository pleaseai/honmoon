{
  "name": "@honmoon/cli",
  "type": "module",
  "version": "0.1.0",
  "bin": {
    "honmoonctl": "src/index.ts"
  },
  "scripts": {
    "build": "bun build src/index.ts --target=bun --outdir dist",
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@honmoon/policy": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "~6.0.2"
  }
}
