{
  "name": "honmoon-mono",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "lint": "bun run --filter '*' lint",
    "dashboard:dev": "bun run --filter '@honmoon/dashboard' dev"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
